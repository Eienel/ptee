# Required. Public endpoints cannot complete a scan.
# This value is compiled into the client bundle and is PUBLIC — restrict the key
# to your domain in your provider's dashboard, or put a proxy in front of it.
VITE_RPC_URL=https://your-endpoint.example.com/v2/your-key
